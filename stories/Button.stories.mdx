import { ArgsTable, Meta, Story, Canvas } from '@storybook/addon-docs';
import { Button, Props } from '../src/Button';

<Meta title="MDX/Button" component={Button} />

export const Template = args => <Button {...args} />

# Button
Let's define a story for a `Button` component

<Story name="My Button" args={{ children: 'Hello from MDX btn' }}>
  {Template.bind({})}
</Story>

<ArgsTable story="My Button" />

I want axtra text description here!

<Canvas>
  <Story name="Secondary" args={{ children: 'Hello from MDX btn', variant: "secondary" }}>
    {Template.bind({})}
  </Story>
</Canvas>